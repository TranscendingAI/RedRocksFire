---
/**
 * Legacy slug redirect — /about/mission-vission-values →
 *                        /about/mission-vision-values (permanent).
 *
 * Created 2026-09 when the typo in the slug was corrected. Anyone with the
 * old URL (search engines, external links, bookmarked pages) gets a 301 to
 * the canonical page. Delete this file once analytics show zero traffic
 * to the old slug (typically 6–12 months after the rename).
 */
return Astro.redirect('/about/mission-vision-values', 301);
---
